spring:
  application:
    name: eureka-server
server:
  port: 1111
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    serviceurl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    register-with-eureka: false

